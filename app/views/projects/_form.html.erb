<div class="row">

  <div class="large-8 columns">

  <%= form_for(@project) do |f| %>
    <% if @project.errors.any? %>
      <div id="error_explanation">
        <p><%= pluralize(@project.errors.count, "error") %> prohibited this article from being saved:</p>
        <ul>
        <% @project.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :technologies_used %><br />
      <%= f.text_area :technologies_used %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>

  </div>
</div>
